# This project uses branch-specific env files — do not use a single shared .env for secrets.
#
#   cp .env.dev.example .env.dev
#   cp .env.uat.example .env.uat
#   cp .env.production.example .env.production
#
# Then: git checkout dev|uat|master && npm run dev
# Docs: docs/operations/environments.md
